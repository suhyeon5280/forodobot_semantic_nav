# Dependencies for the OmniVLA-edge policy process only.
#
# Install these into a SEPARATE conda env from the SDK server — the server runs
# on Python 3.9 with pyppeteer, and mixing torch into it buys nothing.
# environment-policy.yml wraps this file, so from the repo root:
#
#   conda env create -f environment-policy.yml
#   conda activate rover-policy
#
# Or into an existing env: pip install -r policy/requirements.txt
#
# torch stays a pip install (not a conda package) and stays unpinned — recent
# GPUs need the newest CUDA build, and conda-forge's pytorch tends to lag.
#
torch
torchvision
numpy
pillow
requests
efficientnet_pytorch
utm

# CLIP ViT-B/32 is the text encoder OmniVLA-edge was trained against.
# Not on PyPI under this name; install from source.
ftfy
regex
tqdm
clip @ git+https://github.com/openai/CLIP.git
