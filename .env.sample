SDK_API_TOKEN="3x4mpl3t0k4n"
BOT_SLUG="weld-arm-ultron"
# Optional. Leave unset to let the server pick: installed Google Chrome first,
# then Playwright's bundled Chromium.
# CHROME_EXECUTABLE_PATH="/path/to/chrome"
MAP_ZOOM_LEVEL=18
# Mission mode. Keep this COMMENTED OUT for autonomous driving: when it is set,
# every endpoint returns 400 "Call /start-mission endpoint to start a mission"
# until you POST /start-mission.
# MISSION_SLUG="mission-ex4mpl3"
TTS_PROVIDER="edge"
TTS_API_KEY=""
TTS_VOICE="en-US-GuyNeural"
