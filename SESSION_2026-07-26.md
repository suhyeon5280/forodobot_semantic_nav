# Frodobot 세션 정리 — 2026-07-26

Earth Rovers SDK 기반 주행 데이터 수집 실험을 점검하고, 발견된 버그를 고치고, 블루투스 컨트롤러 조작을 추가한 작업 기록.

## 1. 기존 실험 구조 파악

- `main.py` (uncommitted): `/dataset/start`, `/dataset/log-frame`, `/dataset/stop`, `/dataset/status` 엔드포인트로 세션 단위 주행 데이터 기록 (카메라 프레임 10Hz + control 1:1, GPS/IMU 별도 ~1Hz)
- `static/keyboard_control.html`: WASD/화살표 키 조작 + 녹화 버튼
- `upload_to_hf.py`: 녹화된 세션 폴더를 Hugging Face Hub에 데이터셋으로 업로드
- `dataset/sessions/` 확인 결과: 세션 2개는 이미지 0장(실패), 1개(`2026-07-20_23-28-25`)는 682프레임 정상 기록

## 2. 버그 — 조작 중 `window.sendMessage is not a function`

- **원인**: `browser_service.py`의 `initialize_browser()`에 락이 없어서, 짧은 시간에 겹치는 `/control` 요청들이 헤드리스 브라우저를 동시에 두 번 띄우는 레이스 컨디션 발생. 나중에 완성된 페이지로 `self.page`가 덮어써지면서, 아직 `basicRtm.js` 로딩이 안 끝난 페이지를 참조해 에러 발생.
- **수정**: `browser_service.py`에 `asyncio.Lock` 추가, `initialize_browser()`를 그 락으로 감쌈.

## 3. GPS 값이 `latitude/longitude=1000`, `fix_quality=0`으로 나오는 이유

- 서버 코드 문제 아님. `/data`는 로봇이 RTM으로 방송하는 원본 값을 그대로 전달할 뿐이며, 해당 값은 GPS 모듈이 위성 fix를 못 잡았다는 표준 sentinel/NMEA 코드.
- 해결책은 하드웨어 쪽: 실외 이동 후 대기, `fix_quality`/`gps_signal`이 오르는지 폴링으로 확인.

## 4. 버그 — 조작이 "됐다 안됐다" 함

원인 3가지 확인:
1. `static/basicRtm.js`의 `sendMessage()`가 fire-and-forget이라, 실제 RTM 전송 성공/실패가 서버까지 전달되지 않음 (항상 HTTP 200 응답)
2. RTM 연결 끊김(`ConnectionStateChange`)에 대한 재연결 로직 없음
3. `auth_response_data`가 프로세스 전역 캐시라 RTM 토큰이 만료돼도 갱신 안 됨

**수정 내역:**
- `static/basicRtm.js`: `sendMessage()`가 Promise를 `return`하고, 실패 시 `throw`
- `browser_service.py`: `send_message()`가 `return window.sendMessage(message)`로 그 Promise를 실제로 await — 실패 시 예외가 `main.py`의 `/control`까지 전파되어 HTTP 500으로 드러남
- `browser_service.py`: 헤드리스 페이지의 `console` 이벤트를 서버 stdout으로 포워딩 (`[browser console:type] text`) — RTM 연결 상태 변화, 전송 실패 등이 이제 서버 로그에서 보임

*(RTM 재연결 로직 / 토큰 자동 갱신은 아직 미구현 — 필요 시 추가 가능)*

## 5. 서버 종료(Ctrl+C) 시 뜨는 크래시 로그

- **원인**: pyppeteer가 자체 `SIGINT` 핸들러를 등록해서, hypercorn의 asyncio 이벤트 루프 종료 처리와 충돌 (`RuntimeError: This event loop is already running` → `Event loop is closed`)
- **확인**: 실제로는 좀비 프로세스 없이 정상 종료됨 (hypercorn/헤드리스 크롬 모두 깨끗이 종료 확인됨)
- **조치**: 기존 버그로 원인만 규명, 수정은 보류 (필요 시 `main.py`에 정식 shutdown 훅 추가해서 조용히 만들 수 있음)

## 6. 블루투스 "Wireless Controller"(DualShock4) 지원 추가

- 신규 파일: `static/controller_control.html`
- 브라우저 Gamepad API로 왼쪽 스틱 → `linear`(전/후진)/`angular`(좌우 회전) 아날로그 조작
- `buttons[9]`(Options)로 녹화 시작/정지 토글
- `keyboard_control.html`과 동일한 `/control`, `/v2/front`, `/dataset/*` 엔드포인트 재사용 — 백엔드 변경 없음

**버그 — "No controller detected"만 뜸:**
- 원인: `gamepadconnected` 이벤트에만 의존 → 이벤트가 안 뜨는 타이밍/환경에서 감지 실패
- OS 레벨 확인(`bluetoothctl`, `/dev/input/js0`)은 정상이었음 → 브라우저 쪽 코드 문제로 결론
- 수정: 매 프레임 `navigator.getGamepads()`를 직접 스캔하는 `getActiveGamepad()`로 변경

## 7. 출력(속도) 제한

- `keyboard_control.html`, `controller_control.html` 둘 다 `SPEED_SCALE = 0.5` 상수 추가 → 최대 출력 50%로 제한
- 필요시 각 파일 상단의 `SPEED_SCALE` 값만 수정하면 조절 가능

## 변경된 파일

```
 M browser_service.py               (락 추가, console 포워딩, sendMessage await)
 M static/basicRtm.js               (sendMessage가 Promise 반환/throw)
 M static/keyboard_control.html     (SPEED_SCALE 추가)
?? static/controller_control.html   (신규: 게임패드 조작 페이지)
```

`main.py`, `upload_to_hf.py`, `dataset/`는 이번 세션 이전에 이미 만들어져 있던 것으로, 이번 세션에서 직접 수정하지 않음.

**전부 아직 git commit 전 (uncommitted) 상태.**

## 8. `/sdk`에서 GPS가 안 잡히는 문제 (재확인)

- 위 3번과 동일 결론을 별도 대화에서 재확인: `hypercorn --bind 127.0.0.1:8000` 등 서버 바인딩 설정은 GPS와 무관.
- GPS/위치는 로봇 하드웨어가 Agora RTM 데이터 채널로 브라우저에 방송하는 값 (`static/basicRtm.js`의 `MessageFromPeer` → `window.rtm_data`) → `/data`가 그대로 반환.
- `dataset/sessions/`의 기존 3개 세션을 직접 열어 확인: **전부 `gps_signal: 0`, `latitude`/`longitude: 1000`(fix 없음 sentinel), `fix_quality: 0`**. 이미지+control 로깅은 정상 (10Hz, 1:1 동기화).
- `gps_imu.jsonl` 스키마에 `hdop`, `fix_quality`, `distance_to_rtk_station`, `gps_ttff` 필드가 있는 것으로 보아 RTK 보정 지원 하드웨어로 추정.
- 진단 방법: `curl http://127.0.0.1:8000/data`로 필드 유무/값 확인, 브라우저 콘솔에서 `"AgoraRTM peer msg ... received"` 로그 여부로 로봇이 아예 데이터를 안 보내는지 vs GPS fix만 안 잡히는지 구분.

## 9. VLA 기반 라스트 마일 내비게이션 — 데이터 수집 파이프라인 설계

목표: 현재 이미지 + 과거 이미지 히스토리 + 실시간 OSM map 기반 컨텍스트 → 바로 앞 trajectory 예측 → Frodobot mini 주행.

**선행 조건 — GPS fix 확보 (8번 항목의 블로커)**: trajectory 라벨과 OSM 정렬이 전부 실측 좌표에 의존하므로, fix 안 잡힌 세션(현재 3개 전부)은 궤적/맵 학습에 쓸 수 없음. 실외에서 `fix_quality`/`gps_signal` 확인 후 수집 시작 필요. RTK 기지국 커버리지 확보 시 sidewalk 레벨 정밀도 가능.

**권장 설계:**

1. **샘플링 레이트 정합** — `main.py`의 `_gpsimu_poll_loop`가 `asyncio.sleep(1.0)`로 1Hz 고정, 이미지는 10Hz라 궤적 디테일 손실. `browser_service.data()`는 캐시 읽기라 비용 거의 없으므로 poll 주기를 이미지에 맞춰 5~10Hz로 올리는 것 권장 (단, 로봇의 실제 RTM push rate가 그만큼 되는지 먼저 확인).
2. **프레임 단위 join** — 현재 `control.jsonl`(이미지+조향)과 `gps_imu.jsonl`(위치)이 분리 로그. 학습 전처리 단계에서 timestamp 기준 nearest-join 필요.
3. **future trajectory 라벨** — 사람이 주행한 GPS 궤적을 hindsight relabeling으로 future waypoint 라벨 생성. **lat/lon degree 그대로 쓰지 말고 UTM/ENU로 변환한 ego-frame(현재 위치·heading 기준 상대좌표, 미터 단위)** 로 표현해야 VLA가 실제로 예측할 값이 됨.
4. **OSM map 입력은 수집 시점에 이미지로 저장하지 않기** — 프레임마다 Leaflet 스크린샷 찍으면 용량/속도 문제. 대신 pose(lat/lon/heading)만 정밀 로깅하고, 학습 데이터 생성 시점에 오프라인 미러링한 OSM 타일(`.mbtiles`)에서 heading-up으로 크롭/rasterize.
5. **주행 다양성** — 막다른 길 복귀, 장애물 회피 등 recovery 상황도 의도적으로 수집 (imitation learning distribution-shift 대응).

*(아직 코드로 구현하지 않음 — 다음 단계로 GPS/IMU 폴링 레이트 수정, 프레임-포즈 join 스크립트, OSM 오프라인 타일 크롭 파이프라인 중 우선순위 논의 예정)*
